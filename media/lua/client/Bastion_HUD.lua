-- ============================================================
-- Bastion_HUD.lua  (media/lua/client/)
-- DEPRECATED — replaced by BastionWindow (Bastion_Window.lua).
-- Kept as a no-op stub so any saved references don't error.
-- ============================================================
print("[Bastion] HUD stub loaded (replaced by BastionWindow)")

BastionHUD = BastionHUD or {}
function BastionHUD.show()   end
function BastionHUD.hide()   end
function BastionHUD.toggle() end
